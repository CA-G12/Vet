export default interface IAuth {
    name?: string;
    password?: string;
    confirmPassword?: string;
    role?: string;
    showPassword?: boolean;
    showConfirmPassword?: boolean;
    email?: string;
  }
